# 作業方針 0813

## 1. 目的

棋譜解析を「AI候補との一致表示」から、現在棋力、局面別の弱点、成長推移、次の学習テーマまで確認できる成長診断へ拡張する。

## 2. 実装方針

- 投稿者本人の手だけを集計し、先手・後手とも投稿者視点へ正規化した評価値を使う。
- 既存の着手後解析と5候補表示を壊さず、本人の着手前だけ追加解析して実着手と比較する。
- mate scoreは通常の評価値平均へ混ぜず、詰み維持・詰み逃しとして別集計する。
- 1局の集計結果を保存し、直近10局・30局・100局のサマリーと履歴は保存済み集計から算出する。
- 級段、信頼度、損失上限、局面区分、総合・終盤の重みは設定ファイルへ集約する。
- 推薦はサービス境界へ分離し、将来ルールベースからLLMへ差し替えられるようにする。
- 詰めろ・必至は確実なエンジン信号が得られるまで未表示とし、推測値を表示しない。

## 3. フェーズ

### Phase 1：既存コード調査

モデル、解析サービス、やねうら王MultiPV、投稿者側、評価値正規化、API、棋譜再生、SVG評価値グラフ、CSS、テストを確認した。既存解析は各実着手後の局面を保存するため、実着手の候補順位判定には着手前解析の追加が必要と判断した。

### Phase 2：棋力計算

上限付き平均評価値損失、mate別扱い、第1候補・上位3候補一致、悪手・大悪手、序盤・中盤・終盤、勝勢維持、劣勢回復を実装した。最善と次善の差が80以下で次善を選んだ場合は損失を候補差まで緩和する。

### Phase 3：終盤力

終盤評価損失、詰み発見、詰み維持、詰み逃し、勝勢維持、上位候補一致から100点満点で計算する。詰み機会ごとの手数と状態をJSONで保存する。

### Phase 4：DB・API

`analysis_results` に着手前候補とmate情報を追加し、`game_skill_analyses` を追加した。棋力推定作成・取得、サマリー、履歴、終盤、推薦APIを追加した。

### Phase 5〜7：画面・推移・推薦

棋譜再生画面へ「棋力推定」を追加し、専用画面へ遷移する。10/30/100局、総合結果、詳細、SVG推移グラフ、終盤力、詰み記録、最大3件の推薦をレスポンシブ表示する。

### Phase 8：検証

- バックエンド：現在ソースをDockerへマウントして `78 passed`。
- Python構文検査：成功。
- TypeScript：`tsc -b` 成功。
- Vite：既存 `frontend/dist` の所有権問題を避け、`/tmp/comment-kaiseki-skill-build` への本番ビルド成功。
- 専用lintスクリプト：プロジェクトに未定義。

## 4. 現在の推定値の性質

現在の棋力推定は、実棋力ラベル付き棋譜から学習したモデルではない。評価値損失、一致率、局面別精度、勝勢維持、回復、詰み結果を重み付けした暫定ヒューリスティックである。画面にも「本アプリ独自の推定値」「公式レーティングではない」と表示する。実データ収集後は設定値と換算式を較正する。

## 5. 残課題

- 既存棋譜は着手前解析を保持していないため、棋力推定前に再解析が必要。
- 詰みの「発生後、数手遅れて発見」の連続追跡は、現在は各本人手の詰み維持判定が中心。局面系列をまたぐ厳密な発見遅延へ拡張する。
- 詰めろ・必至検出は未実装。`TrainingRecommendationService` 付近のTODO境界から追加する。
- レーティング換算と重みを実棋力ラベル付きデータで較正する。
- フロントのコンポーネントテスト環境を導入し、専用画面の操作テストを追加する。
