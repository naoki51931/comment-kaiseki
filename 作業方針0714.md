# 作業方針 0714

## 1. 目標

まず報酬を伴わない小規模な試作版を完成させ、10〜30人のテストで次の仮説を検証する。

1. KIFから重要局面を安定して3〜5件抽出できるか。
2. 3つの質問から、本人の狙いと誤認を含む有用なコメントを集められるか。
3. 人による審査を現実的な時間と費用で運用できるか。

報酬処理や高度な不正検知は、この検証後に追加する。

## 2. 開発原則

- 小さく動く単位で実装し、各段階で実棋譜を使って検証する。
- 将棋エンジンの判断と、コメント品質・不正リスクの判断を分離する。
- コメント提出前に最善手や読み筋を見せない。
- 評価値は投稿ユーザー視点へ正規化し、元値と変換後の値を追跡可能にする。
- 審査、報酬、取消は状態遷移として実装し、監査可能にする。
- AI判定は審査補助に限定し、単独で却下を確定させない。
- 個人情報の収集は段階に応じた最小限にする。

## 3. 優先順位

### フェーズ1：報酬なし試作版

- メール認証付きユーザー登録
- KIFアップロードと内容検証
- 指し手列の正規化とSHA-256重複検知
- やねうら王による非同期解析
- ユーザー視点の評価値、勝率、読み筋の保存
- 連続候補を統合した重要局面3〜5件の抽出
- 3問形式のコメント入力と下書き保存
- コメント提出後のエンジン解説表示
- 管理者による承認、差し戻し、却下

完了条件は、テスト用KIFを投稿してから審査結果を確認するまで、一連の操作ができることである。

### フェーズ2：手動報酬版

- 1棋譜300円の報酬台帳
- 1日2件、月20件などの上限管理
- 口座情報の暗号化とHMACフィンガープリント
- 3,000円以上の振込申請
- 管理者向け振込CSVと支払記録
- リスクスコアと報酬保留

開始条件は、税務・契約・個人情報・振込手段を専門家へ確認し、利用規約と同意画面を確定していることである。

### フェーズ3：品質・不正検知

- コメントの再利用・類似度検知
- 部分棋譜と類似指し手列の検知
- 入力時間、貼り付け回数などの補助シグナル
- 品質スコアと審査支援要約
- 審査理由・品質タグの集計

### フェーズ4：AI棋譜コーチ

- 承認済みコメントのみをJSONLへ出力
- 局面、棋力、戦型、失敗理由による検索
- 類似局面RAGと初心者向け解説
- ユーザーごとの弱点分析

## 4. 最初に実装する縦切り

```text
KIF投稿
  → 形式検証・正規化・重複確認
  → 解析ジョブ登録
  → 評価値保存
  → 重要局面抽出
  → 3問に回答
  → 管理者が審査
  → 結果とエンジン解説を表示
```

API、画面、データベースを機能単位で同時に通し、各層だけを先に大量実装しない。

## 5. 技術方針

- フロントエンド：React、TypeScript、Vite。UIライブラリは画面設計確定後にMaterial UIを導入する。
- API：Python、FastAPI、Pydantic、SQLAlchemy、Alembic。
- DB：PostgreSQL。
- ローカル開発・テストはDocker Composeを標準とし、PostgreSQL、API、フロントエンドを同じ構成で起動する。ホストへ直接依存パッケージを導入しない。
- 非同期処理：Redis + Celery。ローカル開発では同期実行も選べるようにする。
- 棋譜：python-shogiで読み込み、内部表現はUSI指し手列と各手前後のSFENを基準にする。
- 解析：USIアダプター越しにやねうら王を呼び出し、エンジンを交換可能にする。
- 保存：開発時ローカル、本番S3。アップロードファイルは非公開とする。

## 6. 重要局面抽出の初期実装

候補スコアは、評価値差、勝率差、形勢逆転、詰み状態を組み合わせる。候補同士が前後3手以内なら、重要度が最大の1件へ統合する。しきい値を満たす局面が3件未満なら、変化量の大きい順に補充し、最大5件を必須回答とする。

抽出結果には、採用理由、使用したしきい値、エンジン名・バージョン、探索条件を保存し、後から再現できるようにする。

## 7. データと状態管理

- `games.analysis_status` と審査・報酬状態を混在させない。
- 報酬は元帳形式で履歴を残し、ゲーム単位の一意制約で二重付与を防ぐ。
- コメントの提出後は回答時点のスナップショットを保持する。
- エンジン解説の閲覧開始時刻を記録し、提出前の漏えいを監査できるようにする。
- 承認済みかつ利用同意の範囲内のデータだけを学習・RAG用に出力する。

## 8. セキュリティと運用

- パスワードはArgon2id、短命アクセストークンと更新トークンを使う。
- 口座情報はアプリケーション層で暗号化し、鍵をDBと分離する。
- 管理者は多要素認証を必須とし、権限ごとにAPIを制限する。
- 棋譜ファイルはサイズ、文字コード、実内容を検査する。
- IPや端末情報は利用目的と保存期間を明示し、必要最小限にする。
- バックアップ復元、報酬取消、CSV再出力を運用手順としてテストする。

## 9. 直近の作業順

1. SQLAlchemyモデルとAlembic初期マイグレーションを作る。
2. KIFパーサーと正規化ハッシュをテスト駆動で実装する。
3. Redis/CeleryとUSIエンジンアダプターを追加する。
4. 重要局面抽出ロジックを独立した純粋関数として実装する。
5. コメントフォームと提出前非公開制御を実装する。
6. 管理者審査と監査ログを追加する。
7. 実棋譜セットで抽出精度と審査時間を計測する。

## 10. 未決事項

- 振込手数料を運営負担とするか、報酬から控除するか。
- 棋譜・コメントの著作権および学習利用許諾の文言。
- 退会時やサービス終了時の最低額未満の未払報酬。
- KIF以外の形式を追加する順序。
- 評価値から勝率への変換式と棋力別のしきい値。
- 審査担当者1人あたりの目標処理時間と二重審査の条件。


## 11. 実施済み作業

### 2026年7月13日：初期スキャフォールド作成

- `/home/ubuntu/comment_kaiseki` をプロジェクトディレクトリとして作成した。
- プロジェクト共通の作業規約を `AGENTS.md` に記載した。
- 構成、起動方法、初期API、重要な前提を `README.md` に記載した。
- 本文書 `作業方針0714.md` にMVPの優先順位、技術方針、直近の作業順を整理した。
- PythonやNode.jsの生成物、秘密情報、依存パッケージを除外する `.gitignore` を作成した。

### バックエンド

- FastAPIとPydanticを使ったMVP用バックエンドを作成した。
- `main.py` を25行に抑え、FastAPIアプリの生成、CORS設定、ルーター登録だけを担当させた。
- APIルーターを `app/api/router.py` と `app/api/routes/` へ分割した。
- APIスキーマを `app/schemas.py`、サンプルデータを `app/store.py` へ分離した。
- ヘルスチェック、棋譜一覧・投稿、重要局面一覧、報酬サマリーの初期APIを実装した。
- 棋譜投稿時に、本人対局・未投稿であることへの同意を必須にした。
- サンプル棋譜、重要局面3件、報酬サマリーをインメモリデータとして用意した。
- ヘルスチェック、棋譜一覧、不明な棋譜IDのテストを作成した。

### フロントエンド

- React、TypeScript、Viteを使ったMVP用フロントエンドを作成した。
- コメント待ち件数、審査中報酬、確定報酬、振込可能額を表示するダッシュボードを作成した。
- 棋譜ファイル、対局日、先後、本人対局確認を入力する投稿フォームを作成した。
- 最近の棋譜と解析状態を表示する一覧を作成した。
- API通信の送信中、空状態、エラー状態を表示するようにした。
- 開発用APIプロキシとレスポンシブ対応の初期デザインを作成した。

### 検証結果

- `npm install` でフロントエンド依存関係を導入した。
- `npm run build` によるTypeScript型チェックとVite本番ビルドは成功した。
- 分割後のPythonファイルは `python3 -m py_compile` による構文検査に成功した。
- Docker内のpytestでKIFパーサーと投稿APIの12件がすべて成功した。

### 当時の制約（後続実装で解消済み）

この時点では、やねうら王、Redis/Celery、認証、コメント審査、報酬・振込、コメント入力画面、管理者画面が未実装だった。これらは第14節のMVP縦切り実装で追加済みである。

## 12. 2026年7月13日：永続化・KIF投稿の実装

### 今回実装した範囲

- Docker Composeをローカル開発の標準実行方式とし、PostgreSQL 17、FastAPI、Viteの3サービスを定義した。
- バックエンドのDockerfileを追加し、コンテナ起動時に `alembic upgrade head` を実行してからAPIを起動するようにした。
- SQLAlchemy 2.xのDB接続とモデルを追加した。
- ユーザー、棋譜、各手の解析結果、重要局面、コメント提出、3問の回答、審査履歴、監査ログを別テーブルとして定義した。
- `games.analysis_status` は解析状態だけに限定し、審査状態を `comment_submissions.review_status` へ分離した。
- PostgreSQLに対するAlembic初期マイグレーションを生成した。
- 投稿APIを実ファイル受信へ変更し、拡張子、2MB上限、空ファイル、文字コード、KIF構文、開始局面、指し手の合法性を検証するようにした。
- KIFはUTF-8（BOM対応）またはShift_JIS（CP932）を受け付け、`python-shogi` でUSI指し手列へ正規化する。
- 重複検知用SHA-256は「開始局面SFEN＋USI指し手列」から生成し、ファイル名、対局者名、文字コードなどのメタデータには依存させない。
- 原本は公開ディレクトリ外へUUID名、ディレクトリ権限700、ファイル権限600で保存する。本番では同じストレージ境界をS3実装へ差し替える。
- 同一ハッシュにはDB一意制約を設定し、事前確認と一意制約違反の両方をHTTP 409として処理する。
- 認証実装までの暫定措置として、投稿はDB上の `prototype@example.invalid` ユーザーへ紐づける。この処理は認証導入時に現在ユーザー取得へ置換する。
- フロントエンドはファイル名だけでなく選択されたKIF本体をmultipartで送信するようにした。
- SQLite分離DBを使うAPIテストとKIFパーサー単体テストを追加し、Docker内で12件の成功を確認した。

### この時点で未実装だった範囲

以下は当時の記録であり、分散トランザクション補償を除いて第14節で実装済みである。

- メール認証、Argon2id、アクセストークン・更新トークン、および実ユーザーへの所有者制御。
- Redis/Celeryのジョブ登録、USIエンジンアダプター、やねうら王解析、失敗時の再試行。
- 解析結果からの重要局面抽出、コメント回答・提出、管理者審査のAPIと画面。
- S3ストレージ実装、マルウェア検査。
- 報酬・振込機能（フェーズ2）。

DBコミット後のCelery投入失敗を回復するOutbox方式は、第15節の作業で実装済みである。

## 13. 複数棋譜形式fixtureと読み込みテスト

- `backend/tests/fixtures/formats/` を形式別読み込みテストの標準fixture置き場とする。
- `.kif`、`.ki2`、`.csa`、`.txt` の正常系ダミーファイルを配置し、いずれも平手初期局面から同じ3手を表現させる。
- 形式別パーサーを実装し、pytestをパラメータ化して全fixtureの読み込み成功を確認する。
- 全形式について、USI正規化結果、開始局面SFEN、指し手数、SHA-256が一致することを検証する。
- `.txt` は曖昧な拡張子であるため、受け付ける内容を「1行1手のUSI指し手列」として確定してからAPIで許可する。
- 投稿APIはKIF・KI2・CSA・TXTに対応済みで、4fixtureを同じUSI列へ正規化するテストも有効化済みである。

## 14. 2026年7月13日：MVP縦切り・認証・報酬の実装

### 実装した範囲

- Docker ComposeへRedis、Celery worker、Mailpitを追加した。
- USIエンジン境界、やねうら王アダプター、開発用決定的評価器を追加した。
- SFEN、手番視点の生評価値、投稿ユーザー視点評価値、勝率、読み筋、エンジン情報、探索条件を保存する。
- 解析失敗を最大3回まで指数バックオフで再試行し、回数とエラーを記録する。
- 評価値差、勝率差、形勢逆転、詰みを用いる重要局面抽出と近接候補統合、3〜5件補充を純粋関数として実装した。
- 3問コメントの下書き、必須検証、一括提出、提出スナップショット、審査状態を実装した。
- 提出前は評価値・採用理由をAPIから返さず、提出後だけ表示する。
- メール確認、Argon2id、JWT、更新トークンローテーション、所有者制御、管理者MFAを実装した。
- 開発時はMailpitへ24時間有効な確認リンクを送信し、未確認ユーザーのログインを拒否する。
- 管理者の承認・差し戻し・却下、状態遷移検証、品質タグ、監査ログを実装した。
- KIF、KI2、CSA、1行1手USIテキストを共通USI列へ正規化し、4fixtureの同一ハッシュを検証した。
- 承認時の300円報酬台帳、ゲーム単位一意制約、日次・月次上限、保留・解除・取消を実装した。
- 口座情報のアプリ層暗号化、HMAC、振込申請、管理者CSV、支払記録を実装した。
- ローカル保存とS3 private・SSE保存を切り替え可能にし、ClamAV INSTREAM検査を追加した。
- GitHub Actions CI、PostgreSQLバックアップ・復元スクリプト、本番用Nginxイメージを追加した。

### 外部資材が必要な本番接続

- やねうら王の実バイナリと評価関数ファイルを配置し、実棋譜で探索性能を測定する。
- 本番SMTP、S3資格情報・バケット、ClamAVサービスを用意して各切替設定を有効化する。
- 公開ドメイン、TLS証明書、秘密鍵管理、クラウドの受信規則を設定する。
- GitHubへリポジトリを接続してCIを実行し、本番バックアップの保管先と復元訓練を設定する。

### 検証結果

- Docker内pytestは30件すべて成功した。解析DB結合テストではSFEN、評価値、勝率、読み筋、重要局面、状態遷移、後手投稿時の詰み評価正規化まで確認した。
- KIF投稿からRedis/Celery解析、重要局面3件抽出までPostgreSQL環境で成功した。
- 3問下書き・一括提出、提出前非公開、提出後表示をE2E確認した。
- Mailpitへの確認メール送信、未確認ログイン拒否、確認リンクのHTTP 303をE2E確認した。
- React/TypeScript/Vite本番ビルドは成功した。


## 15. 2026年7月13日：現在の実装状況と残課題

### 現在実装済み

- Redis/Celery解析、USIエンジン境界、やねうら王プロセス呼び出し、リトライ・失敗状態管理。
- SFEN、投稿者視点評価値・勝率、読み筋、エンジン情報、探索条件の保存。
- 後手投稿時および詰み評価の投稿者視点正規化。
- 重要局面抽出、3問コメント、認証・所有者制御、管理者審査、4棋譜形式対応。
- S3・ClamAV切替、報酬・振込バックエンド、CI・バックアップ定義。
- バックエンドテスト36件。
- Stripe Checkoutによる月額1,000円・初回30日無料のAI解説プラン、署名検証Webhook、Customer Portal、評価値の無料表示と、AIコメント・読み筋の購読中のみの公開制御。

### 現在の残課題

- 利用者の口座登録・振込申請画面と管理者CSV出力は実装済み。管理者の支払済み記録画面を作る。
- 保存期間、退会、孤立ファイルに対応する削除処理を実装する。
- フロントエンドテスト、ブラウザE2E、実Redis・実やねうら王・PostgreSQLの継続的な結合テストを整備する。
- やねうら王バイナリと評価関数、本番SMTP・S3・ClamAV、TLS、監視、バックアップ保管先を実環境へ接続する。
