# 実装状況・運用引継ぎ（2026-07-14）

これまでの実装、現在の稼働構成、確認結果、残課題をまとめた引継ぎ資料です。認証情報や秘密鍵の実値は記載しません。

## 現在の構成

- React 19.2.7、Vite 8.1.4、Nginx静的配信
- FastAPI 0.139.0、Starlette 1.3.1、複数Uvicorn worker
- PostgreSQL、SQLAlchemy、Alembic
- Redis、Celery、Outbox
- やねうら王 NNUE 9.60git 64AVX2、水匠5評価関数
- 公開先: `http://18.130.141.50`（現時点はHTTP）

## 実装済み機能

### 認証・権限

- メール確認付き登録、確認メール再送、期限切れトークン再発行、送信回数制限
- Argon2id、短命アクセストークン、ローテーション更新トークン
- 棋譜の所有者制御、管理者権限、MFA、管理者作成CLI
- 特定テストユーザー向けの評価値・有料状態切替

### 棋譜投稿・管理

- KIF、KI2、CSA、TXTのアップロードとクリップボード貼り付け
- 自動形式判定、USI正規化、SHA-256重複検知
- 棋戦名、先手名、後手名の読込・表示
- 棋譜、解析、下書き、保存ファイルの一括削除
- 報酬記録済み棋譜の削除防止

### 解析・重要局面

- Redis/Celery解析、リトライ、失敗状態管理、Outbox再投入
- やねうら王USIアダプターとプロセス制御
- SFEN、投稿者視点の評価値・勝率、詰み、読み筋、探索条件、エンジン情報の保存
- 後手投稿時を含む投稿者視点への評価値正規化
- 評価値差、勝率差、逆転、詰みによるスコアリング
- 前後3手以内の候補統合、3〜5局面への補充・制限
- USI指し手のKIF日本語表記
- 最大5候補、各候補最大5手の読み筋

主な設定は `ENGINE_KIND=yaneuraou`、`ENGINE_NODES=100000`、`MultiPV=5`、`USI_Hash=64MB`、`Threads=1`、定跡なしです。1棋譜につき1プロセスを使用し、解析完了後に終了します。

### 盤面・コメント

- 棋譜選択とコメント入力の間に盤面再生画面を実装
- 盤面、持ち駒、手数、評価値、分岐、重要局面コメントを連動
- 盤面80%表示、全マス同一サイズ
- 3問回答、下書き、必須検証、一括提出、スナップショット
- 提出前の解析情報非公開と提出後の解説表示
- スマホヘッダーの2段化、中央揃え、メール折返し、ボタン整列
- 視認性の低い文字色を修正

### AIコメント

- 3問回答と同一手数の過去回答からコメントを自動生成
- 有料ユーザーによる修正
- 修正前後、差分、手数、ユーザー、参照回答IDを保存し監査ログへ記録
- 次回の同一手数生成では最新修正を優先

現在の「学習」は保存回答と修正履歴を再利用する検索・フィードバック方式で、外部LLMのファインチューニングではありません。

### 課金・審査・報酬

- 初回30日無料、その後月額1,000円のAIプラン
- 評価値は無料、AIコメント・読み筋・分岐は有料
- Stripe Checkout、顧客ポータル、Webhook、テスト課金切替
- 管理者審査一覧・詳細、承認・差し戻し・却下、品質タグ、監査ログ
- 300円報酬台帳、二重付与防止、上限、保留・確定・取消
- 口座暗号化、振込申請、CSV、支払記録

### 保存・セキュリティ・運用

- ローカル非公開保存とS3 private保存の切替
- ClamAV接続、保存とDB登録の障害補償
- 内部サービスのホスト向けポート公開停止
- Nginxセキュリティヘッダー
- JWT・暗号化・口座フィンガープリント鍵のランダム化
- CI、DBバックアップ・復元スクリプト

## メール設定

Gmail SMTPでは次の環境変数を設定します。

- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_USERNAME`
- `SMTP_PASSWORD`
- `MAIL_FROM`
- `PUBLIC_BASE_URL`

アプリパスワードなどの秘密値はGitや文書へ記載せず、サーバーの環境ファイルまたはシークレット管理に保存します。

## DB変更と既存データ

- 棋戦名・先後名・複数読み筋のマイグレーションを適用済み
- AIコメント・フィードバック用マイグレーション `e5f7a9b2c304` を適用済み
- 既存の提出済み棋譜へAIコメントをバックフィル済み
- 保存済み棋譜の再解析で134局面の分岐保存を確認済み

## 確認結果

- バックエンドテスト: 47件成功
- フロントエンド本番ビルド: 成功
- Alembic差分: 新規upgrade操作なし
- `npm audit`、`pip-audit`: 既知脆弱性0件
- backend、frontend、worker: 起動確認済み

## 残課題

1. HTTPS/TLSと正式ドメイン。現在のHTTP通信はパスワードとトークンが暗号化されません。
2. 本番SMTPの到達性、迷惑メール判定、再送運用の継続確認。
3. S3・ClamAVの本番接続、保存期限・退会時削除、孤立ファイル清掃。
4. ログ集約、監視、アラート、バックアップ保管と復元訓練。
5. Reactコンポーネント、ブラウザE2E、実Redis/Celery・PostgreSQL・やねうら王の継続的結合テスト。
6. Celery workerの非root化などコンテナ運用強化。
7. 必要に応じたAIコメントの埋め込み検索・外部LLM対応。

## Weaviate横断検索（2026-07-14）

- 棋譜メタデータ、重要局面、提出済み3問回答、AIコメントを所有者別に索引します。
- 日本語GSEトークナイズを使用したWeaviate BM25検索です。検索結果から対象棋譜・手数へ移動できます。
- `GET /api/search?q=...`、`POST /api/search/reindex` を追加しました。
- Weaviate停止時はPostgreSQLの部分一致検索へフォールバックします。
- 他ユーザーの棋譜と未提出コメントは検索対象外です。Weaviateはホストへポート公開しません。
- 設定は `WEAVIATE_ENABLED`、`WEAVIATE_URL`、`WEAVIATE_API_KEY`、`WEAVIATE_TIMEOUT_SECONDS` です。

## 注意事項

- SMTPアプリパスワード、JWT鍵、暗号化鍵、Stripe秘密鍵をコミットしないでください。
- エンジンと評価関数のライセンス・配布条件を本番公開前に再確認してください。
- 合法手数や終局直前の局面では、読み筋が5手未満、候補が5本未満になる場合があります。
- 解析が古い場合はworkerが最新イメージ・設定で起動しているか確認してください。

詳細な変更経緯は [作業方針0715.md](./作業方針0715.md) を参照してください。
