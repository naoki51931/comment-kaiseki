# AGENTS.md

## プロジェクトの目的

このリポジトリでは「棋譜コメント研究所」を開発する。ユーザー本人の将棋棋譜を解析し、重要局面への内省コメントを収集・審査して、承認された棋譜に報酬を付与するサービスである。

## 作業時の基本ルール

- 会話、仕様書、画面文言、コメントは原則として日本語を使う。
- 実装前に `README.md`、`作業方針0715.md`、必要に応じて履歴の `作業方針0714.md` を確認する。
- MVPでは KIF 投稿、非同期解析の受付、重要局面へのコメント、手動審査を優先する。
- ユーザーが回答を提出する前に、エンジン最善手や詳しい読み筋を公開しない。
- 評価値は必ず投稿ユーザー側から見た符号に正規化する。
- 金額は浮動小数点数ではなく整数の円単位で扱う。
- 報酬確定、取消、支払処理には監査ログを残す。
- 銀行口座番号などの秘密情報をログ、テストデータ、Git管理対象へ入れない。
- AI文章判定のみを理由に投稿を自動却下しない。

## ディレクトリ

- `backend/`: FastAPI API、ドメインロジック、テスト
- `frontend/`: React + TypeScript のユーザー画面
- `作業方針0715.md`: 現在の優先順位、作業単位、完了条件
- `作業方針0714.md`: これまでの段階計画と実装履歴

## 実装規約

### Backend

- Python 3.11以上、FastAPI、Pydanticを使用する。
- APIの入出力は型を定義し、HTTPエラーを明示する。
- 棋譜解析、報酬、審査、不正検知のロジックをルーターへ直接埋め込まない。
- データベース変更はAlembicマイグレーションで管理する。
- 外部エンジンやGPT呼び出しは差し替え可能な境界を設ける。
- 変更時は関連するpytestを追加または更新する。

### Frontend

- TypeScriptの厳格モードを維持する。
- APIレスポンス、フォーム値、状態には型を付ける。
- 送信中、空状態、失敗状態を画面に表示する。
- アクセシビリティを考慮し、フォームにはラベルを付ける。

## 検証

変更内容に応じて以下を実行する。

```bash
cd backend && pytest
cd frontend && npm run build
```

検証できなかった項目は、理由とともに作業報告へ記載する。

## 禁止事項

- 実在する個人情報や銀行口座情報をfixtureへ保存しない。
- 未承認コメントを学習データとして出力しない。
- 重複検知をファイル名や対局者名だけで行わない。
- 報酬状態を履歴なしで上書きしない。
- 法務・税務・決済事業者の確認事項を、実装上の推測だけで確定扱いしない。

