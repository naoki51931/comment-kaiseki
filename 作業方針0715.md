# 作業方針 0715

現在までの実装内容と運用引継ぎは [実装状況20260714.md](./実装状況20260714.md) を参照すること。本書は詳細な変更経緯と作業判断を保持する。

## 1. 目的

MVPのバックエンド縦切りは一通り動作している。7月15日は、保存済みデータを利用者・審査担当者へ正しく届ける画面を完成させ、メール認証と解析ジョブ投入の障害時にも回復できる状態へ近づける。

優先する成果は次の3点とする。

1. コメント提出後に、重要局面の評価・採用理由・読み筋を安全に閲覧できる。
2. 管理者が棋譜と3問回答を確認して審査できる。
3. メール送信失敗とCelery投入失敗から、データ不整合なく回復できる。

## 2. 現在の到達点

- KIF・KI2・CSA・USIテキストを共通USI列へ正規化できる。
- Redis/Celery、USIエンジン境界、やねうら王呼び出し、リトライ・失敗状態管理を実装済み。
- SFEN、投稿者視点の評価値・勝率、読み筋、エンジン情報、探索条件を保存済み。
- 重要局面抽出、3問回答、下書き、一括提出、提出スナップショットを実装済み。
- メール認証、JWT、更新トークン、所有者制御、管理者MFAを実装済み。
- 管理者審査、報酬・振込バックエンド、S3・ClamAV切替を実装済み。
- バックエンドテスト45件が成功している。
- Stripe Checkoutによる月額1,000円・初回30日無料のAI解説プランを実装済み。
- 検証用課金切替は対象ユーザーの名前横に表示し、白文字を避けた濃色文字と薄い金色の背景で視認性を確保する。

## 3. 優先順位

### 優先1：提出後のエンジン解説表示

解析結果に保存されている読み筋を重要局面へ関連付け、提出後だけAPIレスポンスへ含める。

実装内容：

- 重要局面と同じ手数の解析結果から読み筋を取得する。
- 未提出時は評価値、採用理由、読み筋をすべて非公開にする。
- 提出済みの場合だけ読み筋を返し、フロントエンドで表示する。
- 読み筋が空の場合の表示を用意する。
- 他ユーザーの棋譜を取得できないことを維持する。

完了条件：

- 提出前APIでは `principal_variation` が `null` になる。
- 提出後APIでは対象局面の読み筋が返る。
- 画面で評価値、採用理由、読み筋を確認できる。
- 公開制御のAPIテストが成功する。

### 優先2：管理者審査詳細画面

一覧のIDだけで審査せず、棋譜情報、重要局面、3問回答、提出日時を確認して判断できる画面を作る。

実装内容：

- 管理者専用の審査詳細APIを追加する。
- 回答を局面・質問番号順に表示する。
- 承認、差し戻し、却下時に理由を必須にする。
- 品質タグを選択できるようにする。
- MFA済み管理者以外のアクセスを拒否する。
- 審査履歴と監査ログの既存記録を維持する。

完了条件：

- 管理者が回答内容を読んでから審査操作できる。
- 不正な状態遷移はHTTP 409または422で拒否される。
- 一般ユーザーは審査詳細を取得できない。
- 審査APIと画面の失敗状態が表示される。

### 優先3：確認メール再送

メール送信失敗や期限切れ時に、ユーザーが同じメールアドレスで回復できるようにする。

実装内容：

- 確認メール再送APIと画面を追加する。
- 未使用の旧トークンを無効化して新しい24時間トークンを発行する。
- 登録済みかどうかを第三者へ過度に開示しないレスポンスにする。
- 短時間の連続送信を制限する。
- 認証済みユーザーには新規トークンを発行しない。
- SMTP失敗時のDB状態と再試行方法を明確にする。

完了条件：

- 期限切れ後に再送した新しいリンクで認証できる。
- 古いリンクは利用できない。
- 連続送信が制限される。
- メール送信失敗後も再送操作が可能である。

### 優先4：解析ジョブ投入の障害補償

DBコミット後にRedis/Celeryへの投入が失敗しても、棋譜が永久に `QUEUED` のまま残らない仕組みを導入する。第一候補はDB Outbox方式とする。

実装内容：

- 棋譜作成と同じトランザクションで解析依頼Outboxを保存する。
- dispatcherが未送信OutboxをCeleryへ投入する。
- 重複配送されても解析結果が壊れないよう冪等にする。
- 送信回数、最終エラー、送信日時を記録する。
- 長時間 `QUEUED` の棋譜を再投入する運用コマンドを用意する。

完了条件：

- Redis停止中でも棋譜投稿自体は整合した状態で保存される。
- Redis復旧後に未送信ジョブが投入される。
- 同じジョブの再配送で解析結果が重複しない。
- 障害・復旧を再現する結合テストが成功する。

## 4. 次点の作業

優先1〜4の後に次を進める。

- 利用者の口座登録・振込申請と管理者CSV出力は実装済み。管理者の支払済み記録画面を追加する。
- 保存期間に基づく削除、退会時削除、孤立ファイル清掃。
- ReactコンポーネントテストとブラウザE2E。
- 実Redis、PostgreSQL、やねうら王を使う継続的な結合テスト。
- ログ集約、監視、アラート、TLS、本番SMTP・S3・ClamAV接続。

## 5. 実装原則

- ユーザーが回答を提出する前に、評価値、最善手、読み筋を公開しない。
- 評価値と詰み評価は必ず投稿者視点へ正規化する。
- API入出力には型を定義し、失敗状態を明示する。
- 解析、審査、報酬の状態遷移をルーターへ直接埋め込まない。
- 管理者操作、報酬変更、支払処理には監査ログを残す。
- 外部サービス障害を握りつぶさず、再試行可能な状態を保存する。
- AI文章判定だけを理由に投稿を自動却下しない。

## 6. 検証手順

変更単位で次を実行する。

```bash
sudo docker compose run --rm --no-deps backend pytest -q
sudo docker compose run --rm --no-deps frontend sh -c "npm ci && npm run build"
sudo docker compose run --rm backend alembic check
```

DB変更を含む場合は、PostgreSQLでマイグレーションのupgradeとdowngradeを確認する。画面変更では、送信中、空状態、失敗状態、権限拒否を確認する。

## 7. 7月15日の完了判定

優先1〜4を実装済み。読み筋表示、審査詳細、確認メール再送、解析Outbox再送を追加し、バックエンド45件とフロントエンドビルド、Alembic差分なしを確認した。

作業終了時に、実装済み機能、残課題、テスト件数、本番外部資材の必要事項をREADMEと本書へ反映する。


## 8. 7月14日セキュリティ更新

依存パッケージを既知脆弱性のない固定バージョンへ更新し、JWT鍵ローテーション、MFA再暗号化、本番用Nginx・Uvicorn構成、内部サービスのポート非公開化を実施した。バックエンド45件、Alembic差分なし、npm audit・pip-auditとも既知脆弱性0件を確認した。HTTP公開は残るためHTTPS化を次のインフラ最優先項目とする。

- 所有者向け棋譜削除APIと確認付き削除ボタンを追加。関連解析・コメント・保存ファイルを削除し、報酬台帳がある棋譜は会計整合性のため拒否する。

- 重要局面のUSI表記を局面からKIF日本語表記へ変換。既存棋譜もAPI応答時に変換し、対象棋譜の80手目 `4a7a` が `７一飛(41)` になることを確認した。

- 棋譜選択とコメント入力の間に棋譜再生画面を追加。各手の盤面・持ち駒・日本語指し手、提出後の評価値、AIプラン有効時の読み筋分岐、重要局面の3問コメントを連動表示する。

- `AI_ACCESS_TEST_USER_EMAIL` の対象ユーザーだけ棋譜再生の評価値表示ボタンを利用可能にした。対象ユーザーは提出前の評価値を手動表示でき、検証用月額1,000円課金がONなら読み筋・分岐も表示する。

## 2026-07-14 追加実装：棋戦情報・5分岐解析

- 棋戦名、先手名、後手名をKIF/KI2/CSA/TXTから取得し、DB・API・最近の棋譜・棋譜再生へ反映。
- やねうら王USIアダプターをMultiPV 5へ拡張。候補ごとに最大5手の読み筋、評価値、詰み情報を保存。
- 読み筋分岐は課金中のみ表示し、局面評価値は無料表示を維持。
- マイグレーション `d4e6f8a1b203` を適用済み。既存2棋譜のメタデータを再読込済み。
- 自動テスト46件成功。React本番ビルド成功。
- 外部準備：やねうら王バイナリ・評価関数はサーバー上に未配置のため、現在の実解析は開発用評価器。配置後に `ENGINE_KIND=yaneuraou` へ切替し、実棋譜で性能・タイムアウトを検証する。

- 5候補の読み筋は選択式タブで表示し、選択した候補ごとに評価値と最大5手先までの予測を確認できる。局面移動時は候補1へ戻る。

## 2026-07-14 読み筋・評価値の不具合調査

- 原因: Celery workerが古いイメージかつ `ENGINE_KIND=development` で、保存済み解析の `variations` は0件だった。開発用評価器は駒得のみなので実戦的な評価値にはならない。
- workerを最新コードで再構築。Composeに `ENGINE_KIND`、`YANEURAOU_PATH`、`ENGINE_NODES` の外部設定と `./yaneuraou:/opt/yaneuraou:ro` を追加。
- 実やねうら王と対応NNUE評価関数が未配置のため、既存棋譜の再解析は未実施。配置後に再解析する。

## 2026-07-14 やねうら王・水匠5 本番導入

- 公式やねうら王ソースからLinux AVX2版（YaneuraOu NNUE 9.60git 64AVX2）をビルド。公式Releaseの水匠5 `nn.bin` と `FV_SCALE=24` を配置。
- workerを `ENGINE_KIND=yaneuraou` に切替。USI_Hash=64MB、Threads=1、MultiPV=5、BookFile=no_book。
- USI出力を専用スレッドとキューで読み、1棋譜につき1エンジンプロセスを再利用するよう修正。
- 棋譜再生画面に実際の「やねうら王／水匠5」とエンジンバージョンを表示。
- 未提出棋譜ID 3を再解析し、134局面すべてに評価値と5候補を保存。
- 自動テスト46件成功。

- 探索量を100,000ノードへ増加。棋譜ID 3は29.5秒で再解析完了し、134局面すべてに5候補を保存。終局付近など探索上PVが短い候補は最大5手として表示する。

- MultiPVの同一候補で短い速報PVが長いPVを上書きしないよう、最新評価値と最長5手PVを統合。47テスト成功。

- スマホ表示のヘッダーを縦2段・最小高さ196pxへ変更。タイトル、メールアドレス、課金切替、ログアウトを中央揃えし、長いメールアドレスは安全に折り返す。

## 2026-07-14 AIコメント自動生成・修正学習

- コメント提出時、重要局面ごとに3問回答と同手数の提出済み回答を参照してAIコメントを自動生成。
- `ai_comments` に生成文、現在文、参照回答ID、生成方式を保存。
- 課金ユーザーはAIコメントを編集でき、修正前・修正後・文字差分を `ai_comment_feedback` に保存。監査ログも記録。
- 次回、同じ手数のAIコメント生成では最新のユーザー修正文を優先教師として使用。
- 既存の提出済み1棋譜から3件をバックフィル済み。47テスト成功、React本番ビルド成功。
